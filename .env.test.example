# Copy this to .env.test and fill in real values to run live API tests.
# .env.test is gitignored — never commit real credentials.
OCTOPUS_APIKEY=your-api-key-here
# Or use email + password instead:
# OCTOPUS_EMAIL=you@example.com
# OCTOPUS_PASSWORD=yourpassword
