pytest
pytest-asyncio
python-dotenv
aiohttp
# Home Assistant is needed because importing the integration package pulls in HA.
homeassistant
# The API client (PyPI: octopus-kraken; imports as octopy).
octopus-kraken[async]>=0.1.0
